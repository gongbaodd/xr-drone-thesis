\subsection{Integration: Human-AI Collaboration in an XR Environment}

Building on the XR environment and the human-AI collaboration agent developed in prior iterations, this iteration focuses on integrating these components into a unified system.

\paragraph{Design}

In the XR development iteration, a heads-up display (XrJoystickHud) was introduced to provide real-time feedback on the user's joystick inputs. In the iteration, this interface is extended to visualize the AI agent's suggested joystick actions, thereby enabling a direct comparison between user input and agent guidance.

To support this functionality, the structure of the XrJoystickHud was modified to receive data from two emulator components (DroneXrEmulator). One emulator maps joystick data from the XR environment to the YueInputModule (see \cref{fig:quad_structure}), while the other emulator (PidEmulator) retrieves joystick signals generated by the PID controller, as illustrated in \cref{fig:hud}.

\input{figure/hud.tex}

In the evaluation of path following iteration, the agent piloting operation is shaky. To reduce the effect, the frequency was changed to 100 frame one calculation. And the hint solution in the XrJoystickHud was to light up the expected quadrant shown in \cref{fig:integration}.

\begin{figure}[H]
    \caption{Integration of the HAC agent}\label{fig:integration}
    \centering
    \includegraphics[width=0.8\linewidth]{img/integration.png}
\end{figure}

\paragraph{Enactment}

The test play shows that the agent can give correct instruction on the operation. However the hint was shaky occationally. If only following on the instruction of the HUD, the player can not be able to correct operate. Some test play shows that constantly reminding annoy the player.

\paragraph{Analysis}

The result shows a signal that the agent parameter still needs to be refined to avoid shaky solutions. Also the agent should add a condition logic, if the player is correctly piloting towards a correct direction, it should not provide suggestion in case being anoying.